{
  "name": "@relax/backend",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/relaxd",
    "build": "go build -o relaxd ./cmd/relaxd",
    "build:sidecar": "node ./scripts/build-sidecar.mjs",
    "lint": "go vet ./... && (command -v golangci-lint >/dev/null 2>&1 && golangci-lint run ./... || echo 'golangci-lint not installed, skipping')",
    "test": "go test ./..."
  }
}
