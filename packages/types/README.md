# @relax/types

Generated TypeScript types and Connect-RPC client for the RELAX backend.

Generated code under `src/gen/` is **not checked in**. Regenerate with:

```bash
pnpm gen:proto
```

from the repo root.
